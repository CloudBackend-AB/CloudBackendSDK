<application name="Plot My2 app">
  <about publisher="CloudBackend"/>
  <view name="plotMy2" title="My2 map" icon="icon://network/earth_location">
    <panel>
      <map name="mapComp" controls="false" position="fast">
        <rule match="root/row">
          <marker lat="{Lat}" lng="{Long}" title="{Name}"/>
        </rule>
      </map>
    </panel>
  </view>
  <process name="plotMy2 process">
    <trigger view="plotMy2" event="Loaded" step="init"/>
	<step id="1">
	  <!-- ! = read -d parameter from shell -->
      <alias name="dataDoc" value="!"/>
    </step>
    <step id="init">
      <operation name="channel">
        <name>xios</name>
        <title>CloudBackend</title>
        <protocol>XMLFS</protocol>
      </operation>
      <operation name="bind" value="$dataDoc">
        <component name="mapComp" view="plotMy2" />
      </operation>
    </step>
  </process>
</application>    
