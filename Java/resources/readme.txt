Copyright © CloudBackend AB 2021.

The Config.properties contains credentials and preset paths to make the runme.java code more flexible and easier to read.
In the Simple_test files are being uploaded which are contained in this folder (resources).

The com.cbe-source.jar is used for auto completion when writing code. It is however untested. 
On the webb site there is a class tree with all relevant classes created from javadoc and stubb code. 
Please visit https://cloudbackend.com/docs/java/com/cbe/package-summary.html to view how everything is linked together.