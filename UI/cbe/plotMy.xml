<application name="Plot My app">
  <about publisher="CloudBackend"/>
  <view name="plotMy" title="My map" icon="icon://network/earth_location">
    <panel>
      <map name="mapComp" controls="false" position="fast">
        <rule match="root/row">
          <marker lat="{Latitude}" lng="{Longitude}" title="{Text}"/>
        </rule>
      </map>
    </panel>
  </view>
  <process name="plotMy process">
    <trigger view="plotMy" event="Loaded" step="init"/>
	<step id="1">
	  <!-- ! = read -d parameter from shell -->
      <alias name="dataDoc" value="!"/>
    </step>
    <step id="init">
      <operation name="channel">
        <name>xios</name>
        <title>CloudBackend</title>
        <protocol>XMLFS</protocol>
      </operation>
      <operation name="bind" value="$dataDoc">
        <component name="mapComp" view="plotMy" />
      </operation>
    </step>
  </process>
</application>    
